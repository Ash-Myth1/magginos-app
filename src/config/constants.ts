// src/config/constants.ts

// UPI payment recipient — move to an environment variable for production
export const KITCHEN_UPI_ID = 'ashmit.trade@okaxis';

// Menu items are now fetched dynamically from the Firestore 'menu' collection.
// To update the menu, modify the documents in Firebase Console — no code changes needed.
